<form id="app_educateur_delete_{{educateur.id}}" method="post" action="{{ path('app_educateur_delete', {'id': educateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ educateur.id) }}">
</form>
