{% extends 'base.html.twig' %}
{% block title %}
	Licenciés
{% endblock %}
{% block title_btn %}
	<div class="demo-inline-spacing">
		<a href="{{ path('app_licencie_new') }}" class="btn rounded-pill btn-primary">Ajouter</a>
	</div>
{% endblock %}
{% block body %}
	<!-- Basic Bootstrap Table -->
	<div class="card">
		<h5 class="card-header">Table Basic</h5>
		<div class="table-responsive text-nowrap">
			<table class="table">
				<thead>
					<tr>
						<th>Id</th>
						<th>NumLicence</th>
						<th>Nom</th>
						<th>Prenom</th>
						<th>actions</th>
					</tr>
				</thead>
				<tbody class="table-border-bottom-0">
					{% for licencie in licencies %}
						<tr>
							<td>{{ licencie.id }}</td>
							<td>{{ licencie.numLicence }}</td>
							<td>{{ licencie.nom }}</td>
							<td>{{ licencie.prenom }}</td>

							<td>
								<div class="dropdown">
									<button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
										<i class="bx bx-dots-vertical-rounded"></i>
									</button>
									<div class="dropdown-menu">
										<a class="dropdown-item" href="{{ path('app_licencie_show', {'id': licencie.id}) }}">
											<i class="bx bx-show-alt me-1"></i>
											Show</a>
										<a class="dropdown-item" href="{{ path('app_licencie_edit', {'id': licencie.id}) }}">
											<i class="bx bx-edit-alt me-1"></i>
											Edit</a>
										<a class="dropdown-item" href="javascript:void(0);">
											<i class="bx bx-trash me-1"></i>
											Delete</a>
									</div>
								</div>
							</td>


						</tr>
					{% else %}
						<tr>
							<td colspan="4">no records found</td>
						</tr>
					{% endfor %}
				</tbody>
			</table>
		</div>
	</div>
	<!--/ Basic Bootstrap Table -->
{% endblock %}
