{% extends 'base.html.twig' %}

{% block title %}Catégorie
{% endblock %}

{% block body %}
	<div class="col-md-6 col-lg-4 mb-3">
		<div class="card">
			<h5 class="card-header">{{ categorie.nom }}</h5>
			<div class="card-body">
				<blockquote class="blockquote mb-0">
					<p class="mb-0">
						ID:
						{{ categorie.id }}
					</p>
					<p class="mb-0">
						Nom:
						{{ categorie.nom }}</p>
					<p class="mb-0">
						Code raccourci:
						{{ categorie.codeRaccourci }}</p>

				</blockquote>
			</div>
		</div>

		<div class="d-flex flex-row">
			<div class="demo-inline-spacing mt-4 mx-5 d-flex gap-5">
				<a href="{{ path('app_categorie_new') }}" class="btn rounded-pill btn-primary">edit</a>
				<a href="{{ path('app_categorie_index') }}" class="btn rounded-pill btn-outline-primary">back to list</a>
			</div>
		</div>
	</div>
{% endblock %}
